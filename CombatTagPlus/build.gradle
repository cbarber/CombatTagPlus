buildscript {
  repositories { 
    jcenter() 
  }
  dependencies {
    classpath 'com.github.jengelman.gradle.plugins:shadow:1.2.3'
  }
}

apply plugin: 'com.github.johnrengelman.shadow'

description = ''
dependencies {
    // compile group: 'org.mcstats.bukkit', name: 'metrics-lite', version:'R8-SNAPSHOT'
  compile project(':CombatTagPlusCompat-API')
  compile project(':CombatTagPlusCompat-v1_7_R3')
  compile project(':CombatTagPlusCompat-v1_7_R4')
  compile project(':CombatTagPlusCompat-v1_8_R1')
  compile project(':CombatTagPlusCompat-v1_8_R2')
  compile project(':CombatTagPlusCompat-v1_8_R3')
  compile project(':CombatTagPlusCompat-v1_9_R1')
  compile project(':CombatTagPlusCompat-v1_9_R2')
  compile project(':CombatTagPlusCompat-v1_10_R1')
  compile project(':CombatTagPlusCompat-v1_11_R1')
  compile project(':CombatTagPlusCompat-v1_12_R1')
  compile project(':CombatTagPlusHook')
  compile project(':CombatTagPlusFactions-v1_6')
  compile project(':CombatTagPlusFactions-v1_8')
  compile project(':CombatTagPlusFactions-v2_6')
  compile project(':CombatTagPlusFactions-v2_7')
  compile project(':CombatTagPlusWG-v5')
  compile project(':CombatTagPlusWG-v6')
    compile(group: 'org.bukkit', name: 'bukkit', version:'1.10-R0.1-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'me.confuser', name: 'BarAPI', version:'3.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.connorlinfoot', name: 'ActionBarAPI', version:'1.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.inventivetalent', name: 'BossBarAPI', version:'1.0.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.shininet.bukkit', name: 'PlayerHeads', version:'3.9') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.palmergames', name: 'Towny', version:'0.89.2.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.google.code.findbugs', name: 'jsr305', version:'2.0.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}

configurations {
  // exclusions from shadowJar
  runtime.exclude module: 'craftbukkit'
  runtime.exclude module: 'junit'
  runtime.exclude module: 'worldguard'
  runtime.exclude module: 'worldedit'
  runtime.exclude module: 'Towny'
}

shadowJar {
  dependencies {
    exclude(dependency('org.bukkit:bukkit:'))
    exclude(dependency('me.confuser:BarAPI:'))
    exclude(dependency('com.connorlinfoot:ActionBarAPI:'))
    exclude(dependency('org.inventivetalent:BossBarAPI:'))
    exclude(dependency('org.shininet.bukkit:PlayerHeads:'))
    exclude(dependency('com.palmergames:Towny:'))
  }
}
