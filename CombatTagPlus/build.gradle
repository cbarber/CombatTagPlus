apply plugin: 'com.github.johnrengelman.shadow'

group = 'net.minelink'
description = ''
dependencies {
  compile group: 'org.mcstats', name: 'metrics-lite', version:'1.2-SNAPSHOT'
  compile project(':CombatTagPlusParent:CombatTagPlusCompat-API')
  compile project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R1')
  compile project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R2')
  compile project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R3')
  compile project(':CombatTagPlusParent:CombatTagPlusCompat-v1_9_R1')
  compile project(':CombatTagPlusParent:CombatTagPlusHook')
  compile project(':CombatTagPlusParent:CombatTagPlusWG-v6')
    compile(group: 'org.bukkit', name: 'bukkit', version:'1.9-R0.1-SNAPSHOT') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.connorlinfoot', name: 'ActionBarAPI', version:'1.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.inventivetalent', name: 'BossBarAPI', version:'1.0.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.shininet.bukkit', name: 'PlayerHeads', version:'3.9') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.palmergames', name: 'Towny', version:'0.89.2.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'me.confuser', name: 'BarAPI', version:'3.4-SNAPSHOT')
}

shadowJar {
  dependencies {
    include(project(':CombatTagPlusParent:CombatTagPlusCompat-API'))
    include(project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R1'))
    include(project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R2'))
    include(project(':CombatTagPlusParent:CombatTagPlusCompat-v1_8_R3'))
    include(project(':CombatTagPlusParent:CombatTagPlusCompat-v1_9_R1'))
    include(project(':CombatTagPlusParent:CombatTagPlusHook'))
    include(project(':CombatTagPlusParent:CombatTagPlusWG-v6'))
  }
}
